package eu.phisikus.pivonia.pool.mediators

import spock.lang.Specification

class ServerHeartbeatPoolMediatorSpec extends Specification {
    /**
     * TODO
     * 1. Test if new server in the pool is added to the server heartbeat pool
     * 2. Test if server removal removes it from the heartbeat pool
     * 3. Test if transmitter pool assignment is done on heartbeat reception
     * 4. Test if transmitter pool removal is done on heartbeat timeout
     *
     * 5. Check IT tests - remove visitor usage? check if needed
     * 6. Use the server heartbeat pool in IT tests - prove that it works?
     * 7. Unify naming of classes in javadocs & test step comments
     */
}
